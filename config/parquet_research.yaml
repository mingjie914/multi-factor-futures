extends: default.yaml

data:
  source: parquet_futures
  cache:
    enabled: false

# Frozen historical research window. Windows and holding periods are bar counts
# at the selected frequency.
date_range:
  start: '2017-01-01'
  end: '2026-05-31'
