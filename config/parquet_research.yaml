extends: default.yaml

data:
  source: parquet_futures
  cache:
    enabled: false

# Frozen final-OOS research window. Full-history replay must pass explicit dates;
# simulated_live starts on 2026-05-15 and extends only through its dedicated path.
date_range:
  start: '2017-01-01'
  end: '2026-05-14'
