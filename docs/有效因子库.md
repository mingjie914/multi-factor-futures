# 有效因子库

> 当前状态：2026-08-17。运行代码和配置是权威来源；本文只保存当前口径，历史
> 6f/8f/13f 研究定义保留在 `snapshot/` 与 `runs/` 的留存证据中。

## 三个不同的数量口径

| 层级 | 当前数量 | 权威来源 | 含义 |
|---|---:|---|---|
| 固定观察因子集 | 10 | `strategies/combined.py::FACTORS` | 进入比较信号的因子；未通过发布批准，目标权重发布门为 `NO_TARGETS` |
| 已验证候选 | 23 | `config/validated_factors.yaml::validated_candidates` | 可复评候选，不会自动进入生产 |
| 运行时发现池 | 4,060 | 导入 `factors.library` 后的 factor registry | 发现范围，不等于已验证或可交易 |

本次运行时发现池包括 3,024 个 SPEC 展开因子；`factors.library.intraday` 注册
599 个类，其中 585 个属于 `intraday_advanced`。数字会随源码变化，审计时应重新
查询注册表，不能沿用旧报告中的 472、438 或 170 等历史批次数量。

## 当前固定观察 10f

当前组合结构为 38 品种、ICIR窗口参数60/Ledoit-Wolf、Top10/Bottom10、板块 cap=3、
多空分侧 ERC、日度调仓、总暴露 2。它是固定研究和归因基线，不代表目标权重发布批准；
当前固定集合正确性重建的长期夏普为0.544、最大回撤为-22.91%；该结果不等于从当前
有效因子池重新搜索出的最优集合，也不构成目标权重发布批准。

| 因子 | 方向 | 主要信息来源 |
|---|---:|---|
| `intraday_price_peak_count_20d` | +1 | 日内价格峰值 |
| `intraday_realised_skewness_20d` | +1 | 已实现偏度 |
| `intraday_drip_stone_20d` | -1 | 成交量节奏频谱 |
| `intraday_peak_ridge_ratio_20d` | -1 | 脉冲与持续放量 |
| `intraday_torrent_down_20d` | -1 | 下行量价冲击 |
| `intraday_lowest_time_20d` | +1 | 日内低点时刻 |
| `intraday_term_slope_20d` | +1 | 期限结构斜率 |
| `intraday_open_close_volume_ratio_20d` | -1 | 开收盘成交结构 |
| `intraday_turnover_velocity_20d` | +1 | 换手速度 |
| `intraday_price_delay_20d` | -1 | 价格反应延迟 |

10f 来自对13f的清理研究，但该选择使用了受前缀污染的分钟因子，旧逐折证据已经删除。
冻结定义见`snapshot/10f_icir/`；当前固定集合证据位于
`runs/external_guosen_trend_index/20260817_correctness_rebuild/`。不能据此宣称10f已是
当前有效池的最优集合。

## 当前已验证候选 23 个

以下列表与 `config/validated_factors.yaml` 同步：

```text
zero_ret_freq                 open_close_drift
volatility_clustering         oi_vol_corr_daily
oi_time_centroid              wash_trade
settle_position               cross_vol
amihud_vol_ratio              oi_skew_stability
depth_trend                   oi_quantile_range
settle_gap                    amihud_trend
overnight_absorption          session_symmetry
oi_peak_ridge_ratio           seat_long_short_seat_ratio
volume_rank_ratio             herding
volume_time_shape             extreme_freq_balance
term_vol_ratio
```

表中省略统一的 `intraday_` 前缀和 `_20d` 后缀。候选身份只表示已进入当前复评池；
加入生产仍须经过冻结边界、相关性/聚类、成本、容量、滚动样本外和人工审批。

## 更新规则

1. 新因子先注册到发现池，再按 `docs/因子检验与准入流程.md` 执行正式检验。
2. 入池审查显式使用 `|corr| >= 0.5` 的冗余门槛，并人工复核聚类经济含义。
3. 修改 `validated_candidates` 时同步本文；修改生产因子只能通过人工决策更新
   `strategies/combined.py`，不得由研究脚本自动写入。
4. 永久保留holdout ledger和可信不可变run；已被正确性错误否定的派生产物直接清理，
   并在`runs/README.md`登记原因，不能恢复成当前证据。
