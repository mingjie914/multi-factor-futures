# 有效因子库

有效因子库只保存通过正式统计准入的因子，不保存相关簇、组合子集、策略权重或生产状态。

## 当前状态

当前正式有效因子数为 **0**。2026-08-24形成的75条记录来自后来采用的126日IS＋42日OOS
单切分流程，现已从权威库清空；原始证据仍保留在
`runs/factor_validation/20260824_intraday588_is126_oos42_cutoff_20260515/`，仅供历史审计。

旧Markdown名单保存在`factor_library/legacy/`，同样不属于当前有效库。

## 权威文件

- `factor_library/library.json`：程序读取的唯一当前库；
- `factor_library/current.csv`：由JSON导出的人工作业视图；
- `runs/factor_validation/<run_id>/`：不可变完整检验证据。

每条有效记录包含因子名、稳定的`family/category`、注册周期、正式通过周期、方向、IC/t/q、
研究窗口、入库日期和证据哈希。相关聚类的`cluster_id`随样本、阈值和算法变化，只保存在
`runs/factor_selection/<run_id>/`，不得写成因子的永久属性。

## 日常操作

在IDE打开根目录`run_factor_workflow.py`：

1. 默认`VALIDATE_FACTOR_BATCH`：填写本次完整`FACTOR_NAMES`后运行；
2. 首次建库或重建时显式选择`VALIDATE_ALL_INTRADAY`；
3. 审阅正式run后选择`ADMIT_COMPLETED_RUN`入库；
4. 需要相关聚类或组合候选时选择`SELECT_EFFECTIVE_SUBSETS`。

共同周期和单切分观察run不可入库。入库不会修改策略、目标权重或生产批准。完整门槛与
防混用规则以`docs/因子检验与准入流程.md`为准。
