# 独立交易模块验收记录

更新日期：2026-09-16。**交易模块离线验收通过；当期合约/账户参数未齐，正式组合仍禁止发单。**
设计、接口与实施次序见[交易模块设计与验收](交易模块设计与验收.md)。

## 最新：整数目标、阶段独立性与预演边界（2026-09-16）

- balanced在相邻整数内优先贴近理论净名义，再减小权重偏差；保留原方向、20品种和至少一手。
  9月15日权重用于9月16日离线验收，净名义5元，约0.0001%权益；并非未来行情或成交后的保证值。
- 使用现有CSV保留原始权重、理论手数、独立取整、最终目标和校正差额；重复输入得到同一工件。
- 下游命令不再提前导入研究计算。独立子进程阻止研究导入时，size→plan→paper仍通过。
- 非整数目标/委托拒绝而不截断，换月平旧仓要求旧合约规格，Panda远端冻结参数逐项绑定本地订单。
  13个新增反例先失败、修复后通过；交易专项246项通过。真实豆粕历史回包离线复核通过，本轮无新发单。
- 等待进程增加心跳、阶段与退出记录；原异常退出根因仍未知。隔离测试覆盖正常、报错和强制终止。
- 9月16日另行授权的豆粕1手开多/平多已实际成交并核对归零；它只证明该合约通路，不替代20品种渠道验收。
- 9月17日明确仅预演，使用9月16日完整数据，自动发单开关关闭；详见设计文档8.16节。
- 本机运行证据位于runs/trading，账户配置、参数、回报与CSV不随代码提交。

## 历史：完整组合口径修正（2026-09-15）

本节更新此前“500万元取整后17品种、保证金38.95万元”的示例口径，下面旧记录保留为历史验收。

- 新增`complete_unit + units`：先统一缩放至全部非零权重理论手数至少1手，再取整。
  新增`reference + amount`：固定日常折算基准金额，不自动增减规模；原三种金额口径保留。
- `require_one_lot`改为检查取整前手数；同策略多条路由合并后检查，不同策略抵消前分别验收。
  小权重不删除，不单独把不足1手的品种抬成1手；真实账户权益与折算金额分别记录。
- 默认模板及本机Panda案例均使用一份完整组合；实际权益仍500万元，原风险上限及禁发设置保持不变。
- 留存的2026-09-11正式权重实际验证：20个入选品种全部保留、全部理论绝对手数≥1；
  折算基准金额27,320,138.55元；预计保证金2,369,928.90元，占500万元实际权益47.398578%；
  总名义杠杆11.240058倍。组合完整，但未核实规格、总敞口及净敞口风控仍使账户BLOCKED。
- 原始权重字节一致；重复运行目录一致；固定基准与最小组合模式在同一折算金额下的目标CSV字节一致。
  多策略/账户纯折算基准：10策略×5账户、50路由、1900贡献合并190目标，25次中位数19.19ms。
- 全量回归`1215 passed, 22 warnings in 66.46s`；其后仅补充测试边界，无生产代码变更，
  最终交易专项`92 passed in 1.63s`。包括20非零品种另加0权重、多份先放大后取整、
  0.5/0.9手阻断、固定基准跨日变化、部分/全部抵消、超限不截断，以及已有差额与只减仓检查。
- 证据：`runs/trading/20260915_daily_history/complete_unit_acceptance.json`及`pytest_complete_full.log`。
  最新明细目录：`runs/trading/sizing/2026-09-11/822e0222715164bf7c27fe25b1ac8cd9be0aa7f17be60fe877f437a57e95198e/`。
  本次仅本地实现、生成与验证，没有提交委托。

### 保证金口径复核（2026-09-15）

对上述已留存`targets.csv`按方向独立汇总：多头1,347,875.30元（10品种），
空头1,022,053.60元（10品种），合计2,369,928.90元，是未扣交易所优惠的目标毛额。
中金所的IF、TS、TF目标均为空头，该样例不能因大边规则而抵扣这三项。
费率仍为未核实的历史参考值，47.398578%不能认定为柜台真实占用率或资金需求上限。
本次只补充规则范围和展示/实值边界，未调整金额算法、放宽风控或提交委托。

## 已实现的运行链路

`run_trading_workflow.py` 提供互不自动串联的权重、资金折算、差额计划、人工文件、
本地 paper 演练、Panda 快照/冻结预演/明确确认发送/回报核对命令。
默认引用组合目录唯一正式策略；正式身份不等同发布或下单授权。

- 权重产物冻结策略版本、具体合约、数据/代码指纹及逐实例持仓状态；重复输入不改文件。
- 多策略与多账户由路由表连接。同账户同合约先合并名义目标，再对称四舍五入。
- 换策略/换版本保持账户连续性；用最新实际持仓计算差额。旧管理范围通过前次计划承接。
- 资金不足、规格未核实、状态过时等会输出诊断。目标CSV和委托CSV显式标记 `BLOCKED`。
- 超高保证金/非正权益进入只减仓状态；不开新仓，也不默认强平无差额仓位。
- 发送前持久化、账户串行锁、未知或尚未核对的失败回报阻断后续发送。

## 500万元是否足够

**当前样本不能满足全部非零品种至少一手。**

口径：唯一正式策略“多源韧衡(18)”、排名缓冲1、经核对的空仓冷启动；
当前本地认证行情最新完整日为 **2026-09-11**。38个品种中20个非零目标；
按500万元基准权益和原权重折算，17个能取整至至少一手。

| 未达到一手的品种 | 实际合约 | 原始目标手数 | 四舍五入后 |
|---|---|---:|---:|
| 股指IF | IF2612 | -0.2751 | 0 |
| 二年国债TS | TS2612 | -0.4875 | 0 |
| 原油SC | SC2610 | +0.1830 | 0 |

本截面各品种四舍五入后至少一手的权益门槛约 **1,366.01万元**；
按比例未取整前各品种完整达到一手约 **2,732.01万元**，均由SC约束决定。
它们仅是本截面的数量门槛，仍须另检保证金、净暴露和交易限制，并非增资建议。

取整后总名义金额876.36万元、净多134.32万元，净暴露为权益的 **26.86%**，
超过案例配置的10%上限。静态参考保证金约38.95万元，不能代表Panda当日账户保证金。
因此不能因“参考保证金够用”就判定整个正式组合可开仓，也不能强制把三个0手改成1手。

## 已完成的真实本地验证

- 实际CLI串联 `weights → size → panda-snapshot → plan` 成功生成产物。
- 新权重CSV共38行、20个非零目标；首次尾段计算61.55秒，独立CLI重复调用1.77秒，
  重复返回同一权重目录。完整历史计算只用于一次性一致性审计。
- 10个策略实例×5个账户、50条路由的折算约8.52毫秒；1900条路由分量合并为190个账户合约目标，
  与同总资金单路由结果逐项相等。测量结果记录于 `sizing_performance.json`。
- 新SDK worker读取已授权赛事账户：权益/可用各500万元，保证金0，持仓0，活动委托0。
- 账户交易日为9月15日、信号日为9月11日，差额计划正确标记 `STALE_SIGNAL`，
  同时保留规格未核实、三品种0手、净暴露超限等原因。没有发送订单。

本机可复查证据：

- `runs/trading/20260915_design/cli_acceptance.json`
- `runs/trading/20260915_design/acceptance.json`
- `runs/trading/20260915_design/pytest_final.log`
- `runs/trading/20260915_design/validate_real_weights.py`
- `runs/trading/20260915_design/real_weights_validation_final.log`

最终全套测试 **1152 passed**（62.27秒，22条既有数值/弃用警告），编译与diff检查通过。
同起点完整面板与末段：**18因子×38品种×62日**的截面排名、IC、综合得分及
空仓/两种非空状态目标逐值完全一致。提取公共持仓函数前后，**20日账本、目标、实际权重与缓冲诊断完全一致**。
完整面板首次计算1259.47秒，完成后重复审计直接读取受输入契约约束的检查点。
函数内同输入权重复用0.84秒；最终CLI含解释器启动约1.81秒。

最终证据为 `real_weights_validation.json` 与 `acceptance.json` 的 `status=passed`。
此前2015起点旧面板与当前2016起点面板对账存在实际排名差异，输入契约不同，不能互代；
差异摘要保存于 `archival_contract_mismatch.json`，未放宽断言、未修改旧研究证据。
完整核验期间执行层修补导致旧产物代码哈希失效；之后使用最终代码重新出具产物并复核全部断言，
新旧权重CSV字节相同，最终交付目录以 `cli_acceptance.json` 为准。

## Panda安装与取舍

| 组件 | 实际结果 | 用途 |
|---|---|---|
| CLI 0.1.19 | 已安装、OAuth登录和只读自检通过 | 人工查询、自检、排错 |
| Python SDK 0.1.19 | 独立虚拟环境安装、导入/依赖检查、OAuth和快照通过 | 框架程序接入，建议作为主接口 |
| trading skill 0.6.3 | 已安装并读取 | 冻结预演、确认、发送、回报规则 |
| Codex Panda MCP | 已配置但禁用；OAuth服务发现未完成 | 当前不作为交易依赖 |

CLI与SDK使用同一服务及交易语义，两次授权绑定同一赛事身份。
Python SDK位于独立环境，研究环境未安装SDK依赖。MCP登录遇到动态注册地址404，
公开OAuth元数据只在服务前缀下可用；没有绕过服务发现或复制令牌。
CLI与SDK的已验证授权不受这一MCP问题影响。

依据：[CLI安装指南](https://www.pandaaiquant.com/openapi/v1/agent-install-guide/cli)、
[Python安装指南](https://www.pandaaiquant.com/openapi/v1/agent-install-guide/python)、
[官方交易规则](https://www.pandaaiquant.com/openapi/v1/skills/trading/SKILL.md)、
[能力规范](https://www.pandaaiquant.com/openapi/v1/meta/agent-spec)。

## 9月15日当时的验收边界（历史记录）

1. 未执行真实订单；paper测试是理想数量成交，不代表成交速度、滑点或收益。
2. Panda当前空仓。非空持仓字段和今昨仓映射尚无真实样本验证；缺映射会明确拒绝，
   上期所/能源中心今昨平仓不能默认为已自动适配。
3. 实际委托前必须提供核实的当日合约规格、最新完整收盘信号、最新资金与持仓。
4. 带缓冲的多策略净额账户需提供经核对的逐实例状态；真实净额不能自动反推出各策略持仓。
5. 当前只接已核对的中性组合目录。并行开发的只做多扩展尚不在本次交易验收范围内。
6. `config/trading.local.yaml` 与所有账户运行证据仅保留本机；配置已被Git忽略，执行开关关闭。
7. 只减仓表示仅允许平仓指令；实际释放多少保证金以柜台为准，首版不模拟组合保证金优惠。

本次还修正鸡蛋的计价乘数：报价为元/500千克，每手5吨，因此乘数为10；
不能把物理吨数5直接当作价格点乘数。对应单位计算测试已加入。
依据：[大连商品交易所合约文本，第12页鸡蛋合约（华泰期货存档）](https://www.htfc.com/wz_upload/png_upload/20240207/17072856162152a5658.pdf)。
# 每日历史明细扩展验收（2026-09-15）

本日后续增加 `预计占用保证金_元` 与 `account_summary.csv`：明细按路由整数手数估算，
汇总按账户合并目标计算总保证金和占用率。展示采用非零权重数值降序、零权重置底、同权重品种代码排序。
新增保证金与排序验收先得到4 failed、10 passed，实现后交易模块 **68 passed**。
38品种历史样例逐行复核通过；500万元基准权益对应预计保证金389,510.85元、占用率7.790217%。
历史参考规格仍为BLOCKED，原始权重及账户目标CSV与上一版字节一致；本次只扩展报告。
新版证据：`runs/trading/20260915_daily_history/margin_sort_acceptance.json`；
新版目录：`runs/trading/sizing/2026-09-11/b8fc2a4549f42c02f3bbf4d6199157179ec6b200e1066ba19ad29b594cd96472/`。

- 保留步骤1不可变权重；步骤2增加原始权重副本与中文 `daily_positions.csv`，按数据日期和输入哈希归档。
- 测试先验证新增需求失败（7 failed、原有5 passed），实现后交易模块回归 **66 passed**。
  覆盖三类资金、负手数及多空不同保证金、多路由合并取整、同日版本/跨日留存、幂等、日期冲突和篡改校验。
- 用已留存的2026-09-11正式策略权重、500万元配置实际生成38行，其中18行零权重完整保留。
  全部逐行独立Decimal复核金额、单手价值、单手保证金及整数手数；原始权重副本字节一致，重复运行哈希相同。
- 此例使用历史参考规格，状态仍为BLOCKED；IF、TS、SC的非零权重取整后为0手如实保留。
  未重算研究因子、未发送委托。明细中的路由参考手数与账户合并目标分列，不能逐行累计后下单。
- 本机证据：`runs/trading/20260915_daily_history/acceptance.json`；
  示例目录：`runs/trading/sizing/2026-09-11/7988c8e61bb3258c95ca30de002e8354f6141a1b5810aeaef7daf514108ff954/`。
