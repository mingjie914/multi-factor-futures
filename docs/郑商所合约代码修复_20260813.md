# 郑商所合约代码修复审计（2026-08-13）

## 数据处理

2026-07-31日线存在186条郑商所YMM/YYMM别名。别名与四位代码的OHLC、成交量、持仓、
结算价一致，但成交额缩小10,000倍。逐字段核验后删除三位别名、保留四位记录。

修复后对2026年1d、1m、5m、15m全量扫描：郑商所三位代码均为0，同一交易时间与合约
的重复键均为0。`data-health --strict`另在每次运行时检查四个频率最新发布分区，发现
三位代码、相同重复行或冲突重复行时返回`invalid`。

## 代码边界

- `data/contract_symbols.py`统一解析郑商所YMM代码；
- 别名或已四位化代码形成同键冲突时均fail-closed；
- 高频因子热路径不重复执行全量规范化，避免拖慢前端和回测；
- 规范化和重复检查位于Parquet读取边界及独立健康检查。

## 结果处理

删除旧的郑商所别名相关读取缓存约6.83GB，重新生成冻结方案账本、权重、指标和净值图。
旧`20260809_production_compare`还使用分块预热修复前的Runner，故整体删除，不能将其与
新结果的全历史差异全部归因于本次数据修复。

R8两方法冻结验证做了直接同口径复核：替代方法结果完全不变，生产方法夏普仅由
1.3345变为1.3324，四折选择不变。因子搜索开发期截至2024年，且当前有效的完整搜索
已在分块预热修复后运行，因此没有重新执行因子搜索或统计准入。
