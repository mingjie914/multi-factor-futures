# 多因子期货框架研究手册

## 1. 目标

框架服务于收盘后研究和下一交易时段决策，不以发表论文为目标。严谨统计
只负责防止把噪音交给实盘；日常流程必须保持短、明确、可审计。

当前最合适的平衡是三层：

1. 研究层：寻找因子，使用原始 OLS/HAC 和多重检验控制误发现。
2. 验证层：仅对通过因子做 Ridge、成本、风险约束和冻结 walk-forward。
3. 决策层：只读取人工批准的部署包；缺失时输出 `NO_TRADE`。

研究工具可以复杂，收盘决策必须简单。研究报告、旧回测胜者和候选 YAML
都不能直接成为交易配置。

## 2. 固定流程

```text
假设和日期预注册
  -> 点时因子矩阵
  -> 未收缩单因子 Fama-MacBeth OLS + HAC p 值
  -> 全局 Bonferroni
  -> IC / 命中率 / 分层 / 换手 / 分段稳定性
  -> 相关去重
  -> 经济家族上限
  -> 训练期内 Ridge
  -> ATR/波动率风险预算与硬上限
  -> 0.02% 交易成本 + 0.105% 年费
  -> 冻结 walk-forward / 一次性 locked OOS
  -> 人工批准部署包
```

顺序不能交换。Ridge 是组合估计器，不是显著性检验器；Bonferroni 使用
未收缩 OLS/HAC p 值。严格池为空时停止，不为得到回测曲线而放宽门槛。

## 3. 从 JFE 及相关研究引入的方法

已经实际进入代码或治理流程的方法包括：

| 文献思想 | 框架实现 | 边界 |
|---|---|---|
| Moskowitz, Ooi & Pedersen (2012), *Time Series Momentum*, JFE | 多窗口趋势、方向一致性、突破和低换手趋势假设 | 文献提供经济先验，不替代本地检验 |
| Hong & Yogo (2012), *Futures Market Interest*, JFE | OI 动量、异常、波动及价量持仓确认 | OI 缺失时失败关闭 |
| Koijen et al. (2018), *Carry*, JFE | 保留 carry 研究家族和经济分类 | 当前连续合约缺少可靠期限结构，未伪造 carry 数据 |
| Patton & Weller (2020), *Costs of Trading Market Anomalies*, JFE | 换手门槛、净成本和异常衰减意识 | 成本设置为 0.02% + 年费 0.105% |
| Kozak, Nagel & Santosh (2020), *Shrinking the Cross-Section*, JFE | 通过统计治理后使用 Ridge 收缩 | Ridge 不生成筛选 p 值 |
| Jacobs & Müller (2020), JFE，公开后异常衰减 | 分段稳定性、冻结 walk-forward、旧结果不自动复用 | 全历史结果不称为独立 OOS |

此外使用 Deflated Sharpe 和 CSCV/PBO 诊断选择偏差。它们是诊断和发布门槛，
不能挽救没有通过原始统计治理的因子。

## 4. 因子挖掘策略

本轮采用“文献经济先验 + 受限暴力枚举”：42 个不同基础假设、6 个窗口、
6 个变换，共 1,512 个新增 SPEC。选择窗口和变换前一次性冻结，避免看到
局部结果后继续加网格。

没有把遗传规划生成的任意表达式直接注册为正式因子，原因是：

- 表达式搜索本身会制造数量巨大的隐含试验；
- 当前 18,666 个全市场假设已经使 Bonferroni 门槛降至 2.68e-6；
- 没有 2026-07-24 之后的新 holdout 可验证 GP 表达式。

未来若使用 GP，应在单独实验注册表中固定操作符、最大树深、复杂度惩罚和
训练日期，只将少量帕累托前沿表达式带入下一段全新数据；不能让 GP 自动
写入 `config/default.yaml` 或 `config/trading.yaml`。

## 5. 板块与周期

正式板块为：`ferrous`、`nonferrous`、`precious`、`energy`、`agri`、
`stock_index`、`bond`、`other`。股指与国债分开，有色与贵金属分开。

持有期固定为 1/3/5/10/20/40 个交易周期。每个因子可在各板块选择不同的
最优持有期，但选择必须发生在显著性和经济门槛之后。国债、贵金属只有两个
品种时，不做要求至少三个品种的板块截面检验；可另做时间序列规则研究。

## 6. 风险、成本和 Supertrend

组合先按 ATR/波动率分配动态风险预算，再施加单品种、板块、总杠杆和净
敞口硬上限。默认单品种波动预算 2.5%，板块波动预算 6%，硬品种权重上限
100%，总敞口上限 2，净敞口区间为 ±0.5。硬上限是最后保护，不是所有
资产共用 20% 的主要分配规则。

Supertrend 使用 ATR(20) 和 2 倍 ATR，作为与多因子组合并列的 shadow
sleeve，当前资本权重为 0。它不替代风险模型，也不因为规则直观而绕过成本
和 OOS 门槛。

夏普及相关指标无风险利率固定为 0。交易成本为成交名义金额的 0.02%，年化
移仓/管理成本为 0.105%。

## 7. 当前证据和实盘状态

最新 3,111 因子全量历史研究审计有效，但严格通过数为 0；板块局部严格
通过数同样为 0。因此：

- 不执行 Ridge 和组合优化；
- 不生成新的回测曲线；
- 不把 watchlist 放入默认配置；
- `python main.py close` 输出 `NO_TRADE`。

这不是框架“没有用”，而是其最重要的实盘功能正在发挥作用：没有足够证据
时阻止交易。watchlist 只用于预注册下一轮研究。

## 8. 日常操作

```powershell
# 数据健康
python -B main.py data-health --config config/default.yaml

# 研究帮助
python -B main.py research --help
python -B main.py adaptivity --help

# 收盘决策；无批准包时固定 NO_TRADE
python -B main.py close --as-of 2026-07-24

# 验证
python -B -m pytest -q -p no:cacheprovider
```

研究协议与结果位于
`runs/factor_research/expanded_census_3111_2018_20260726/`；已使用的锁定
OOS 证据位于 `runs/locked_oos/`，不得删除或当作未见数据再次调参。
