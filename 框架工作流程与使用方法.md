# 框架工作流程与使用方法

统计门槛以config/default.yaml::validation_policy为唯一参数来源，
策略成员与状态以config/strategy_library.yaml为准，不从历史报告复制另一套默认参数。

## 1. 因子检验与入库

人工intraday创造、GP挖掘、SPEC枚举是并行候选来源，不是默认流程中的串联步骤。
默认入口不加载GP/SPEC目录、不读取GP快照；仅在显式挖掘或接入请求下运行。
候选进入同一检验及后续模块，不能自动入库、加入组合；改写并入intraday需要另行授权。
具体显式入口见[候选接入边界](docs/外接因子接入.md)。

打开run_factor_workflow.py，只修改顶部IDE SETTINGS：

| 分支 | 用途 | 必填/准备 |
|---|---|---|
| VALIDATE_FACTOR_BATCH（默认） | 新增批次正式检验 | FACTOR_NAMES |
| VALIDATE_ALL_INTRADAY | 首次建库/显式全量重检 | 冻结配置及数据 |
| OBSERVE_COMMON_HORIZON | 共同预测期观察，不可入库 | COMMON_HORIZON |
| ADMIT_COMPLETED_RUN | 审阅正式结果后入库 | ADMISSION_RUN_DIR、ADMITTED_AT |
| SELECT_EFFECTIVE_SUBSETS | 从有效库搜索多个组合候选 | 核准SELECTION_START及截止日 |

正式流程：日度截面IC → IC均值NW/HAC统计 → 层级FDR → IC/t/稳健性后置门槛。
相关性去重、组合Walk-forward与locked OOS不是单因子准入条件。
全量与新增批次的多重检验假设族不同，不可混称同一次检验。
细节见[准入流程](docs/因子检验与准入流程.md)。

factor_library/library.json是权威库，current.csv是其导出，禁止分别手改。
数据修补改变冻结区间时应重新检验，不沿用失效证据。
当前151条来自20260908正式605因子重检，旧119/35/75不代表当前有效库。
入库不会自动修改策略成员或批准生产。

## 2. 库内挑选与策略登记

workflows/factor_selection.py由因子入口调用，使用截止日前长历史分阶段验证、
统一组合方法和多路径候选搜索。相关簇用于诊断及多样性，
不将每簇唯一代表作为强制入口，不按固定6/8/10/13规模模板确定“最优”。

搜索输出保存在runs/factor_selection/<run_id>/，审阅后登记冻结成员和方向。
当前五组新候选共享默认方法YAML，成员仅保存在目录factor_sets，不复制五份配置。
旧观察组用config/factor_sets/legacy_*f.json保存定义；可比较，但未通过因子不补录。
命名、分析及搜索局限见[策略候选库](docs/策略候选库.md)。

## 3. 组合回测与比较

打开run_portfolio_workflow.py：

| 分支 | 含义 |
|---|---|
| RUN_PREFERRED（默认） | 运行唯一preferred：多源稳衡 |
| VALIDATE_CONFIGURATIONS | 只校验，不回测 |
| RUN_AND_COMPARE / RUN_AND_COMPARE_ALL | STRATEGY_IDS为空时比较8组非归档候选 |
| RUN_AND_COMPARE_CONFIGURED | 显式使用各YAML模型/风险/优化器，是方法挑战 |
| RUN_AND_COMPARE_SNAPSHOT_AUDIT | 显式复核历史6/8/13，含归档6 |

只选几组时填STRATEGY_IDS。普通比较固定ICIR、Top10/Bottom10、五大分组cap3、
ERC、成本及账本，只改变因子集。方向与有效库冻结证据不一致时拒绝静默替换。
strategies/combined.py内旧10定义是历史兼容基线，不是目录默认17因子组合。

共同H5等分支保留为显式敏感性研究，不是日常路径。
main.py backtest/multi/walkforward/research是各模块兼容/自动化入口；
直接使用旧默认配置不会自动绑定目录首选成员，不能冒充同一策略结果。

## 4. 日期与周期边界

| 内容 | 当前配置/用途 |
|---|---|
| 单因子准入起点 | date_policy.factor_admission_start：2025-01-01 |
| 研究截止日（含当日） | date_policy.research_cutoff：2026-05-15 |
| 长历史组合搜索起点 | SELECTION_START：2016-03-31 |
| 普通回测起点 | date_range.start：2017-01-01，可显式覆盖COMPARISON_START |
| 冻结组合回测终点 | date_range.end：latest_available，最新完整交易日 |

20260909九组对照显式用2016-03-31至2026-09-08，并未改写普通默认起点。
2026-05-15之后标记“模拟实盘观察期”，不是实际成交；
已查看数据不能重新称为未见OOS或反向参与选择。
预热不属于收益统计区间；延长回测须保留全局计算分块边界并验证重叠期一致，
不能只凭滚动天数足够判断缓存等价。

input_bar_frequency强校验原始bar；signal_frequency表示输出更新频率；
best_period是预测期证据。日频因子默认每日共同合成，
H5/H10/H20不机械变成持仓日数。分袖只为显式研究模式。

## 5. 发布与保留

preferred只表示默认回测/观察。config/target_publication.yaml人工批准门目前关闭，
框架不发送交易所订单。正式证据保存到新run目录，不覆盖旧结果。
数据与普通输出留本地；代码、冻结成员定义、有效库及文档纳入Git。
见[维护说明](MAINTENANCE.md)及[产物规则](runs/README.md)。
