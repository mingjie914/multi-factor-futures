# ============================================================
# 多因子框架 — 完整研究与数据源依赖
# ============================================================
# 核心回测依赖以 requirements-minimal.txt 为唯一基线。
# 本文件仅追加当前代码实际使用的可选研究/数据源包。
# ============================================================

-r requirements-minimal.txt

akshare>=1.10.0
dolphindb>=1.30.0
sqlalchemy>=1.4.0,<3
pymysql>=1.0.0,<2
scikit-learn>=1.0.0
matplotlib>=3.5.0
ta_cn[cn]==0.5.2
