# ============================================================
# 多因子框架 — 最简可运行依赖
# ============================================================
# 安装: python -m pip install -r requirements-minimal.txt
# 覆盖: 因子计算 + 检验 + 回测 (不含 ML/自动挖掘/可视化)
# ============================================================

numpy>=1.21.0,<2            # numpy 2.x 与 scipy/pandas 不兼容
pandas>=1.3.0
scipy>=1.7.0,<1.14
pydantic>=1.9.0,<3.0.0
pyyaml>=5.4
pyarrow>=6.0
requests>=2.28.0
# CR-031: MySQL 数据源依赖 (data/mysql_source.py 模块级 import sqlalchemy)
sqlalchemy>=1.4.0
pymysql>=1.0.0
cvxpy>=1.2.0
