# ============================================================
# 多因子框架 — 最简可运行依赖
# ============================================================
# 安装: python -m pip install -r requirements-minimal.txt
# 覆盖: 本地 Parquet + 因子计算/挖掘 + 检验 + 回测
# ============================================================

numpy>=1.24,<3
pandas>=1.5,<4
scipy>=1.10,<2
pydantic>=1.10,<3
pyyaml>=6,<7
pyarrow>=12
duckdb>=1.0,<2
cvxpy>=1.3,<2
