[package]
name = "mf_factor_kernels"
version = "0.1.0"
edition = "2024"

[lib]
name = "_mf_factor_kernels"
crate-type = ["cdylib"]

[dependencies]
numpy = "0.29.0"
pyo3 = { version = "0.29.2", features = ["extension-module"] }

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
